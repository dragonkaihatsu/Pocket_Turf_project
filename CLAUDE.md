# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## リポジトリの性質

プロジェクト名は **Pocket TURF project**（旧 RYUJI）。中身は以下の3つ:

- `README.md` — プロジェクトの入口。名前と構成、ゲームの遊べる内容をまとめてある
- `CLAUDE.md`（本ファイル） — JRA中央競馬・地方競馬の重賞レースを100点満点でスコアリングし、印と買い目プランをHTML形式で出力するための**タスク仕様書**
- `data/` — 出走馬データ・過去データのCSVを置くディレクトリ
- `game/` — 競馬育成ゲーム（下記）

パッケージマネージャもビルド設定もないため、ビルド・lint・テスト用のコマンドは存在しない。主な作業は、`data/` 配下のCSVを入力として読み込み、下記スコアリング仕様に従って分析・出力（HTML）を生成することである。

### `game/pocket-turf.html` — ポケットターフ
DS風の上下2画面で遊ぶ競馬育成ゲーム（単一HTMLファイル、外部依存はGoogle Fontsのみ）。上画面＝Canvasのレース／厩舎描画、下画面＝DOMのタッチUI。予想システムとは独立していて、互いに参照関係はない。

- **構成**: 単一の即時実行関数内に、データ定義 → 馬生成 → `RaceSim`（レース物理） → Canvas描画 → 画面（`V.*`がHTMLを返す） → 操作（`ACT.*`） → `render`/`loop` の順で並ぶ。画面遷移は `show(ビュー名)`、下画面のイベントは `#panel` への単一のクリック委譲で処理する。
- **カレンダーは1年50週**（`WEEKS_PER_YEAR`）、3歳〜5歳の150ターン。重賞は `RACES` の `w` で開催週が固定、条件戦は奇数週のみ生成する。ターン数が多いぶん調教1回の上がり幅は小さく（`doTrain` の係数1.55）、集中育成で主要能力が90台、分散育成で60台に着地するよう調整してある。係数を上げると1年目で頭打ちになる。
- **調教にはランニングコストがある**（`TRAIN` の `cost`）。併せ馬300・プール100・その他50メダル。メダルが足りない調教はボタンごと無効化し、`doTrain` の頭でも残高を確かめてから引く（画面側だけの制御にすると、セーブデータを跨いだときに残高マイナスが通る）。これが売店・馬券・アルバイトと並ぶメダルの出口になっているので、コストを下げると稼ぐ動機そのものが消える。
- **カレンダーの表示は「1月1週」形式**。`MONTH_W`（各月の週数、合計50）と `ymOf(w)` が週番号を月と週に割り、`dateLabel(w)` が文字列にする。週番号そのもの（第N週）は内部の基準として残してあるので、`RACES` の `w` や `SG_RACES` の `w` は通し週のまま扱うこと。
- **賞金はメダルの蛇口**（`GRADE_PRIZE`）。SGは25,000と最も高く、G1は7,000・G2は5,000まで絞ってある。ここをG1側で緩めると売店のブランデー（2,500）が無限に買えるようになり、調教という主要動線そのものが要らなくなる。G1の中でも現実のJRAの賞金比に寄せて `race.prize` で6段に分けてある（`prizeOf`）: 有馬記念15,000 > ジャパンカップ12,500 > 天皇賞春秋・宝塚記念9,000 > クラシック三冠（皐月賞・日本ダービー・菊花賞）8,500 > 大阪杯・安田記念8,000 > その他G1（`GRADE_PRIZE.G1`=7,000）。実際の1着賞金（有馬55,000万・ジャパンC48,000万・天皇賞春秋と宝塚記念22,000万・クラシック三冠20,000万・大阪杯と安田記念15〜18,000万・その他12,000万円、いずれも概算）の並び順をそのまま踏襲し、絶対額はゲームの経済に収まる比率へ圧縮してある。有馬記念(lvl93)・ジャパンカップ(lvl91)・宝塚記念(lvl91)はさらに `race.lvl` で相手のレベルも通常G1(89)より底上げし、格式の高さを賞金と難易度の両方で表す（`fieldLevel`）。SGはG1・G2より稼げるが、そもそも出走条件（G1を2勝）が厳しいので蛇口としては終盤専用。資金繰りは常にぎりぎりで、キャリアの途中でアルバイトを挟む必要が出ることがある。これは想定どおりの緊張であり、`smoke.js` のような自動プレイでも調教ボタンが押せなくなる場面が出る点に注意。
- **売店のアイテム**（`ITEMS`）は週を消費せずメダルで能力を買う。伸ばせるのは成長上限＋`ITEM_OVER`(20)までで、この上限を外すとレースが壊れる。
- **アルバイト**（`JOBS`）はメダルが尽きたときの救済で、`WORK_WEEKS`(4週)を消費して稼ぐ。稼ぎ続けると調教の週がなくなって馬が仕上がらない、という交換が成立している限りバランスは壊れない。現状ピザの配達（期待値750・最低500）が皿洗い（期待値450・最低300）を完全に上回っているので、皿洗いに役割を持たせるなら消費週数を分けるとよい。
- **セーブ**は `localStorage`（`SAVE_KEY`）への自動保存と、12文字のひらがな（4-4-4）の**合言葉**による名前付きスロット。合言葉にセーブ本体は入らない（44^12 ≒ 65ビットしかない）ので、`passId` で英数字IDに落とした保存先を指す鍵として使う。保存先は `passPut`/`passGet` が抽象化していて、`db` ケイパビリティが granted なら端末をまたぎ、無ければ `localStorage` のスロットに落ちる。**現在 `db` は宣言していない**（宣言するとアーティファクトが組織内限定になり公開共有できなくなるため）。切り替えるなら publish 時に `capabilities: {db:{}}` を渡すだけでよく、コード側の変更は要らない。
- **保存は書き込み後に必ず読み返して確かめる**（`save()`）。黙って失敗する環境があり、それは「ページを離れるとデータが消える」形で表面化する。失敗したらその場で画面に警告を出す。あわせて `pagehide`・`beforeunload`・`visibilitychange` でも保存するが、**愛馬がいるときだけ**書く（`autoSave`）。この条件を外すと、タイトル画面を閉じただけで空のデータが既存セーブを上書きする。
- タイトルの「つづきから」はデータが無くても常に表示し、無効理由を出す（ボタンごと消すと、ロード手段が無いように見える）。プライベートウィンドウ等で `localStorage` が使えない環境は `storageOK()` で検出して案内する。
- **隠しレース（SG）**（`SG_RACES`）はカレンダーに載らず、**G1を`SG_NEED_G1`(2)勝**した愛馬にだけ `racesFor` が返す。勝利数は `h.g1w`（G1とSGのみ加算。`h.g1` は重賞全体なので別物）で数え、愛馬ごとに持つので代が替われば一からになる。クラス条件は課さない（cls>=4 を課すと、条件戦を勝っていない馬が自分で開けた扉に入れなくなる）。開催は実在の国際競走の時期に合わせつつ四季に散らしてあり（`SG_TOUR` の4つ）、`seasonOf(w)` が季節を返す。`theme` ごとに上画面の背景を `drawTheme` が描き、`OMEN_RATE` で引く吉兆は演出だけでなく `startRace` で実際に愛馬を強化する。
- **レジェンドカップ**は最後の一戦。四季の4カップを**すべて勝った**馬にだけ第49週に現れる（`legendReady`）。相手は `lvl:108` で `fieldLevel` を上書きし、成長上限まで仕上げた馬だけを集める（他のレースはグレードから決まる）。舞台の `COURSES.レジェンド` は小回りの急坂で、スピードだけでは足りない。**賞金は他のSGと同じ25,000のまま**にしてあり、実利で差をつけるのではなく、難易度だけを最上位に置いた一戦という位置づけ。仕上げきった愛馬で勝率1割強、四季のカップで2割、G1で4〜5割という並びになるよう `lvl` を決めているので、ここを触るときは `legend.js` の勝率表で並びを確かめること。
- **短評**（`HYO`・`hyoOf`）は出馬表の一言コメント。単勝オッズだけで引き、当日の気配や決め手といった隠し値は見ない。**人気を裏切るのがレースなので、短評が当たらないこと自体が味**になる。信頼度を虹色で表し、高いほど赤・低いほど青の11段（歴史創る〜数合わせ）。読み取り欄に文字で出すほか、馬番セルの下に細い色帯を敷いて8頭ぶんの並びを一目で見せる。**枠色は馬番の情報なので色帯で潰さないこと**。
- **馬券は実物のマークシートを模した1画面**（`.sheet`）。式別・馬番・金額の3ブロックを縦に並べ、馬番のセルは枠色で塗る。セルを選ぶと下の `readout` にその馬の名前・脚質・騎手・毛色・オッズが出て、同時に上画面へ大きく描かれる。式別は `app.betType`（`ACT.picktype`）が持つ。出走表を別に並べると下画面がはみ出すので、馬の情報は「選んだ1頭ぶんだけ」を読み取り欄に出す設計を崩さないこと。
- **観戦モード**（`openWatch`/`finishWatch`）は愛馬を出走させずに馬券だけ買う1週。出走馬は `makeWatchField` で8頭すべてNPCを作るため、`sim.me`（カメラと実況が追う馬）は開始時に「いちばん賭けた馬、いなければ1番人気」へ差し替える。強調表示は「愛馬かどうか」ではなく `r===sim.me` で判定すること。
- **配色は単一の世界にコミット**している（鈍い金の筐体＋明るい緑の画面）。テーマ切替はせず、`:root` のトークンだけで全色を決める。Canvas側の芝・空・スタンドの色もトークンと歩調を合わせること。
- **通常のレース背景**（`drawBack`）は昼の空・雲・照明塔・スタンド屋根と、`CROWD` の配色を `hash1()` で散らした6段の観客で埋める。観客の色は毎フレーム乱数を引かず、席のインデックスから決めること（引くとスタンドがちらつく）。芝とダートの色も明るめに寄せてあり、暗くすると画面全体が沈む。
- **馬名は既成句から採る**。`YOJI`（四字熟語121）と `KOTOWAZA`（ことわざ24）を `NAME_POOL` にまとめ、`horseName()` が引く。以前は語を2つ繋いで作っていたが、それだと実在の名馬とぶつかる組み合わせが出るのでやめた。**採用の条件はカタカナ9文字以内**で、これは出走表・着順表・実況のどこに出しても行が折り返さない上限。語を足すときも必ず守ること。**意味が負けや不運を指す語は入れない**（竜頭蛇尾・満身創痍・出る杭は打たれる・河童の川流れなどは一度入れて外した）。自分の愛馬に付く名前でもあるので、勇ましい語・めでたい語・馬にまつわる語を優先する。`horseName` は直近24件を覚えていて同じ名前を続けて返さない（愛馬の候補3頭が同名で並ぶと選べない）。
- **モブ馬名**は `MOB_NAMES`（31頭）。NPCの約45%がこちらを名乗る。実在馬に寄せず、口に出して楽しい語感で選んでいる。同じ名前が2頭並ぶと着順表が読めなくなるので、`makeField`/`makeWatchField` は使用済みの名前を持って引き直す。
- **騎手はレースごとに雇う**（`JOCKEYS` 16人）。脚質4種×手当4段（10/25/40/50%）で、`jockeyChoices(4)` が引いた4人から1人を選ぶ。**手当は腕の値段**（`jSkill` は手当から算出）で、安い若手は出遅れ（30%→トップ3%）と馬群に包まれる不利（`blockMul`）を背負い、高い騎手は直線での引き出しとコーナーの捌きで上回る。得意脚質が愛馬と一致すると `fit` でわずかに伸びる。**手当を導入したぶん賞金の額面を `PRIZE_MUL`(1.5)倍に上げてある**ので、片方だけ触ると経済が壊れる。手当は賞金からの歩合なので、掲示板を外せば発生しない。
- **安い騎手のほうが手取りは多い**（1走あたり約1,600 vs 約1,200）が、勝率は約半分（6.8% vs 11.6%）。**メダルを取るか、勝ち星を取るかの選択**として成立している。昇級・G1勝利によるSG解禁・厩舎ランクはすべて勝ち星で進むので、金だけ見て安い騎手を選び続けるとキャリアが進まない。ここを「高い騎手のほうが手取りも多い」に変えると選択が消える。
- **海外カップの騎手**は `JOCKEY_INT`（9人）で、`INTL_COURSES`（チャイナ・アメリカ・ブリテン・パリ・レジェンド）の舞台でだけNPCに割り当てる。腕は実力級（手当40%相当）で揃えてある。
- **描画ループは常に1本だけ**回す。`render()` は描画のみを行い、`requestAnimationFrame` を呼ばない（`drawScene()` は遷移直後の即時反映用）。ここでrAFを再帰させるとループが多重起動するので注意。
- **連戦の代償**: `raceGap`/`packOf` が前走からの間隔（`h.lastRace` は `absWeek()` で通し週）を見て、中5週以内なら疲労の増分と調子の下げ幅を最大1.9倍まで膨らませる。格の重さは `GRADE_FAT`（条件戦1.00〜SG1.60）で掛ける。あわせて連闘・中詰めの馬は `makeOdds` に渡す倍率が下がり、人気（オッズ）が締まって配当が薄くなる。「使えば使うほど強くなる」を消すための仕組みなので、係数を弱めるならローテーションを組む意味がなくなる点に注意。
- **オープン特別はハンデが累積する**。勝つたび `h.hcp` が1増え、次のOP戦で最大10%まで最高速が削られる（`hcpV`）。OPを踏み台にして稼ぎ続けられないようにする蓋であり、上限を外すと勝てなくなるだけで面白くならない。
- **厩舎は3段階で建て替わる**（`stableRank`・`STABLE`）。最初は暗く湿った板壁にシミとひび、ネームプレートも傾いている（`S.tilt`）。勝ち上がるにつれ白塗りの上等な木材に変わる。特別レース（`h.sg>0`）を勝つと赤と金のレイ（`drawLei`）が掛かる。ここは進捗のごほうびなので、能力値ではなく戦績で段階を決めること。
- **波乱は2つの隠し値で作る**。`form`（当日の気配・全体の最高速）と `kick`（決め手・直線だけ効く）を出走ごとに引く。どちらも `makeOdds` が見る `ratingOf` には入らないので人気に表れず、これが「6〜8番人気が突然伸びる」「1番人気が直線で止まる」の正体になる。**`form` を広げすぎてはいけない**: 実力差はもともと2〜4%しかないので、±7%も振ると実績が無意味になり、しかも消耗が速度の5乗に比例するため上振れした馬ほど失速して走破時計が壊れる（3200mが3:34になった）。着順を動かしたいときは `form` ではなく `kick` を触ること。直線だけなので時計への影響が小さい。消耗の基準速度にも `form` を掛けてあり、これを外すと同じ理由で時計が壊れる。
- **スタミナ切れの演出**（`r.bust`）。残り420m以上を残して `stam` が尽きた馬だけを「バテ」と見なし、ブレーキがかかったように減速させて隊列に赤い札を出す。**ゴール前で出し切るのは普通のこと**なので距離の条件を外してはいけない（外すと全馬が毎レース「バテた」ことになり実況が埋まる。実際に1レース7頭で出た）。失速には下限（`Math.max(fade,0.855)`）を設ける。青天井にすると1着と最下位が20秒以上離れて競馬の絵にならない。`stFac` は距離で底上げしてある（`(d/2000)^0.52`）。これが無いと3200mは全頭がバテ、1600mは誰もバテない極端な絵になる。
- **仕掛けどころが結果を変える**。自馬の勝率は、仕掛けない7% / 直線で仕掛ける14〜18% / 3角から早仕掛け13〜21% / 最初から全開11〜16%。早仕掛けは2000mで25%・2400mで100%の確率でバテる。ここが「追い出す」ボタンの意味なので、`spurt` の消耗倍率(1.55)を下げると押しどころを考える必要がなくなる。
- **レース物理の調整**: 適性・調子は最高速にはわずかしか掛けず（`aptV`/`condV`）、主に消耗（`stFac`）で効かせる。速度に強く掛けると着差が十数秒になり競馬の時計として破綻する。距離係数は実際の持久曲線（Riegel則 `v ∝ d^-0.095`）に合わせてある。
- **コース特性**（`COURSES`）は3つの数値で表す。`straight`（直線距離）はレースのフェーズ配分そのものを決め、短いほど直線での追い比べ区間が短くなって前が残る。`curve`（小回り度）はコーナーでの減速に、`slope`（ゴール前の坂）は残り220mの失速に効く。脚質バランスを触るときは `targetMul` の4フェーズ表と `cStart`（勝負所の開始）を見ること。
- **検証**: `window.PocketTurf` に `RaceSim`・`newHorse`・`makeField` 等を公開しているので、ヘッドレスブラウザから多数のレースを回して走破時計・着差・勝ち脚質の分布を測定できる。バランス変更時は目視でなくこの統計で判断し、実際のJRAの時計（例: 芝2400mのG1で2:22〜2:25）と、コースごとの脚質傾向（2000mの中山・東京・札幌は逃げ先行が6〜7割、新潟1200mは差し・追込が6割超）と突き合わせること。1コースあたり80走では割合の誤差が±10ポイント程度あるので、個々の数値ではなく表全体の並びで判断する。

## 開発ワークフロー

1. ユーザーからCSV（`data/レース名_出走馬.csv`、必要に応じて `data/レース名_過去10年.csv`）を受け取る、またはリポジトリの `data/` に配置されたものを読む。
2. 下記「スコアリング配点」に従って出走馬ごとにスコアを算出し、内訳を明示する。
3. 下記「出力形式（配信フロー）」の順にアウトプット（スコアリング表・HTML card・展開予想・買い目プラン・フィードバックHTML）を作成する。
4. レース結果が判明した際のフィードバック（予測vs実際）から得られた知見は、下記「過去の学習事項」セクションに追記し、配点チューニングの参考として蓄積する。これは本ファイルを更新していくワークフローの一部であり、新しいレースで得られた教訓は都度この節に足していくこと。

## 目的
JRA中央競馬・地方競馬の重賞レースについて、過去データに基づく100点満点のスコアリングを行い、印（◎○▲△注）と買い目プランをHTML形式で出力する。

## データ入力
- 出走馬データはCSV形式で受け取る（例: `data/レース名_出走馬.csv`）
- 想定カラム: 馬番,枠番,馬名,性齢,斤量,騎手,前走着順,前走レース名,上がり3F,調教評価,血統(父/母父)
- 過去10年の同レースデータがあれば `data/レース名_過去10年.csv` として別途読み込む

## スコアリング配点（100点満点）
以下の5項目で85点を構成し、補正項目を加減算して100点満点とする。

| 項目 | 配点 |
|---|---|
| 基礎能力 | 25点 |
| 前走内容 | 20点 |
| コース適性 | 15点 |
| 距離適性 | 15点 |
| 調教 | 10点 |
| **小計** | **85点** |

### 補正項目（加減算）
- **騎手補正**: 実績上位騎手 +1〜+3点、乗り替わりでマイナス経験値なら-1点
- **枠順補正**: 過去10年データの枠別複勝率を参照し、好走枠+2〜+3点、不振枠-2〜-3点
- **前走不利補正**: 前走が展開・コース適性等の外的要因で崩れた場合+1〜+2点
- **高齢馬補正**: 7歳-3点、8歳以上-5点（ただし距離1600m以下のレースは75%に軽減: 7歳-2.25点、8歳以上-3.75点目安）
- **馬場状態補正レイヤー** (±5点): 馬場状態（良/稍重/重/不良）を当日確認し、逃げ馬・道悪巧者は「良馬場スコア」と「重馬場スコア」を2軸で算出する
- **初コース・ぶっつけペナルティ**: 該当コース未経験、または長期休養明けの場合-3点
- **ハンデ戦実績の割引**: 過去の好走がハンデ戦（軽斤量）による場合、本斤量条件・格上馬との対戦経験が乏しい点を考慮し、基礎能力・前走内容を保守的に評価する

## 印（マーク）の付け方
スコア上位から以下を目安に割り振る:
- ◎ 本命（最高スコア）
- ○ 対抗
- ▲ 単穴
- △ 押さえ（複数可）
- 注 警戒（人気を裏切りそうにない程度の注目馬）

## 買い目プラン（標準形式）
特別な指示がない限り、以下をデフォルトとして提示する:

| 券種 | 点数 | 合成オッズ目安 |
|---|---|---|
| 単勝 | 推奨2頭 | 合成3倍前後 |
| ワイド | 3点 | 合成5倍前後 |
| 馬連 | 10点 | 合成5〜7倍前後 |

軸馬（◎の信頼度が特に高い場合）はワイド2点・馬連5〜7点まで絞ってよい。

## 出力形式（配信フロー）
1. **100点スコアリング表**（良馬場+重馬場を併記）
2. **スマホ対応HTML card**（タップで詳細展開、馬番順/スコア順トグル切り替え、ダークテーマ推奨）
3. **ペース別展開予想**（ハイ/ミドル/スロー発生確率と各ペースでの浮上馬）
4. **買い目プラン**（上記の標準形式に沿う）
5. **レース後のフィードバックHTML**（予測vs実際の着順、回収率計算）

## 過去の学習事項（配点チューニングの参考）
- 宝塚記念フィードバックにより「馬場状態補正」「初コースペナルティ」「道悪巧者2軸」「高齢馬補正」を追加
- エルムSで◎テーオーパスワードが3着、1着ルクソールカフェは評価圏内だった → 差し馬の最終直線での脚質加点を要調整
- 新潟記念2026で、新潟大賞典2着（ハンデ戦）の実績があったバレエマスターを見送り、結果的に正解だった → ハンデ戦好走はスコア評価時に「本斤量・格上馬との経験値」を必ず個別チェックする

## タスク実行時の注意
- 過去10年データの枠別・年齢別・上がり3F順位別成績など、統計的裏付けをスコアに反映すること
- 血統データはサンプル数が少ない場合、参考程度に留め過信しない旨をコメントに明記する
- スコア算出根拠（内訳）は必ず馬ごとに表示し、ブラックボックス化しない
